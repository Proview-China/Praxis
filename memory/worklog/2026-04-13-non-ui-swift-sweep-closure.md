# 2026-04-13 non-UI Swift sweep closure

- This closure pass finished the planned non-UI Swift sweep without pulling TUI or GUI into scope.
- Wave 6 truth / provenance residuals are now closed:
  - MP inspection / smoke / readback wording matches actual provenance.
  - mixed provenance, mixed fallback, and override paths have explicit regression coverage.
- The local SQLite runtime baseline now has an explicit schema policy:
  - fresh initialization
  - current-version reopen
  - legacy baseline adoption
  - tampered / future-version rejection
- Wave 7 non-UI entry/export hardening is now closed for the current scope:
  - `PraxisFFI` stays a thin encoded bridge with lifecycle / envelope / snapshot-drain regression coverage.
  - `PraxisCLI` stays a minimal validation harness over `RuntimeGateway -> RuntimeInterface`.
- The blocking TS non-UI tail in `src/agent_core/live-agent-chat.ts` is closed and `npm run typecheck` is green again.
- Final local verification snapshot for this sweep:
  - `swift test` passed with `383` tests in `53` suites.
  - `npm run typecheck` passed.
- Remaining work after this sweep is intentionally outside the current non-UI closure:
  - full provider/browser/multimodal live depth
  - frozen FFI ABI / ownership / streaming protocol
  - Apple UI / TUI / GUI product surfaces
